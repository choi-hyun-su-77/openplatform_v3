<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.v3.core.i18n.mapper.I18nMapper">
    <select id="selectMessages" resultType="map">
        SELECT msg_key, locale, msg_type, message
        FROM platform_v3.cm_i18n_message
        WHERE locale = #{locale}
        <if test="type != null and type != ''">AND msg_type = #{type}</if>
        ORDER BY msg_type, msg_key
    </select>
</mapper>
